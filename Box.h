class Box {
    private:
       int length;
       int width;
       int height;
    public:
       void setLength(int len );
       void setWidth(int widh );
       void setHeight(int heig );
       int getLength();
       int getWidth();
       int getHeight();
       int calcVolume();
};
